import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const ledgerCard = () => {
  return (
    <View>
      <Text>ledgerCard</Text>
    </View>
  );
};

export default ledgerCard;

const styles = StyleSheet.create({});
